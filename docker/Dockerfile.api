# API 服务镜像
FROM python:3.12-slim

WORKDIR /app

# 安装依赖
COPY pyproject.toml .
RUN pip install --no-cache-dir -e . && pip install uvicorn

# 复制后端代码
COPY backend/ backend/

# 暴露端口
EXPOSE 8000

# 启动
CMD ["uvicorn", "backend.app.main:app", "--host", "0.0.0.0", "--port", "8000"]
